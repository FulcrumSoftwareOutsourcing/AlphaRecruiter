function SectionsViewBorder()
{
    //this.CreateSections = function (sections)
    //{
    //    var selected = [];
    //    var items = [];
    //    for (var i = 0; i < sections.length; i++)
    //    {
    //        var bItem = sections[i];
    //        var vItem = new ObservableObject(bItem, ['Id', 'Text']);
    //        var isSelectedByDefault = bItem.IsDefault ;
    //        vItem.BItem = bItem;
            

    //        var imageMeta = Metadata.GetImage(bItem.ImageId);
    //        if (imageMeta)
    //            vItem.ImageUrl = imagesFolder + imageMeta.Folder + '/' + imageMeta.FileName;
    //        else
    //            vItem.ImageUrl = undefined;
    //        //vItem.SectionClassName = ko.observable(isSelectedByDefault ? 'SectionHover' : 'Section');
            

    //        vItem.Selected = ko.observable(isSelectedByDefault);
            
    //        items.push(vItem);

    //        if (isSelectedByDefault)
    //            selected.push(vItem);
    //    }


    //    if (selected.length > 1)
    //    {
    //        for (var i = 0; i < selected.length; i++)
    //        {
    //            selected[i].Selected(false);
    //        }
    //        selected[selected.length - 1].Selected(true);
    //    }
      

    //    return  [items];
    //};
};